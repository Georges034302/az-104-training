#!/usr/bin/env bash
# Shared environment defaults for labs. Safe to source in a shell script.

export LOCATION="${LOCATION:-australiaeast}"
export PREFIX="${PREFIX:-az104}"

echo "LOCATION=$LOCATION"
echo "PREFIX=$PREFIX"
